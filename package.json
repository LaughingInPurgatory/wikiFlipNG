{
  "name": "wikiflip-ng",
  "version": "2.0.0",
  "private": true,
  "description": "WikiFlip NG — Node.js wiki with SQLite content storage",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js",
    "import": "node src/import.js",
    "hash-password": "node src/hash-password.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "markdown-it": "^14.1.0",
    "multer": "^2.0.1"
  }
}
